pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'homework31', url: 'https://github.com/Hronologiya/AQA.git'
            }
        }
        stage('Run Jenkinsfile') {
            steps {
                script {
                    // Ваші наступні кроки
                }
            }
        }
    }
}
